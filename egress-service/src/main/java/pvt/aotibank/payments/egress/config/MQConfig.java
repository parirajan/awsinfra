package pvt.aotibank.payments.egress.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MQConfig {
    // Config logic if needed (e.g. custom JmsListenerContainerFactory)
}
